# 应用名称
spring:
 application:
  name: managementsys
 datasource:
  driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://127.0.0.1:3306/whyx?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
  username: root
  password: root
 mvc:
  view:
   prefix: /WEB-INF/jsp/
   suffix: .jsp
mybatis:
 #Mapper映射文件的位置
 mapper-locations: classpath:dao/**/*.xml
 #需要起别名的所在包的位置
 type-aliases-package: cn.whyx.pojo
# 应用服务 WEB 访问端口
server:
 port: 8888


